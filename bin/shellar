#!/bin/sh

main() {
    local subcommand="${1}"
    shift 1
    local args="${@}"
    if type "shellar-${subcommand}" > /dev/null
    then
        "shellar-${subcommand}" "${args}"
    else
        echo "command not found"
    fi

}

main "$@"
